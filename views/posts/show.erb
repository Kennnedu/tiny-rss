<!DOCTYPE html>
<html lang="en">
  <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link rel=icon href="data:,">
      <title><%= @post[:title] %></title>
    <style>
        :root {
          --white:#fffffe;
          --gray:#eff0f3;
          --black-strong:#0d0d0d;
          --black:#2a2a2a;
          --orange:#ff8e3c;
          --red:#d9376e;
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family:Arial, sans-serif;
            background-color: var(--gray);
            color: var(--black);
        }

        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 0;
            background-color: transparent;
        }

        .article-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .title {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #222;
        }

        .author-time {
            font-size: 14px;
            color: #666;
        }

        /* Smaller floating image */
        .article-image {
            width: 25%;  /* Even smaller, 25% of the container */
            float: left;  /* Floats to the left of the text */
            margin: 0 20px 10px 0;  /* Spacing: top-right-bottom-left */
            height: auto;
            border-radius: 0;
        }

        .description {
            font-size: 16px;
            line-height: 1.8;
            overflow: hidden;  /* Ensures floating doesn't break layout */
            margin-bottom: 20px;
        }

        .action-buttons {
            display: flex;
            justify-content: space-between;
            gap: 10px;
            margin-top: 20px;
            clear: both;  /* Ensures buttons stay below floated content */
        }

        .action-button {
            padding: 10px 15px;
            font-size: 14px;
            font-weight: normal;
            border: 1px solid #ccc;
            background-color: transparent;
            cursor: pointer;
            color: #333;
            transition: background-color 0.3s ease;
        }

        .action-button:hover {
            background-color: #f0f0f0;
        }

        .star {
            color: #ffca28;
        }

        .read-later {
            color: #28a745;
        }

        .copy {
            color: #17a2b8;
        }

        .open-link {
            color: #007BFF;
            text-decoration: none;
            padding: 10px 15px;
        }

        .edit {
            color: #6c757d;
            text-decoration: none;
            padding: 10px 15px;
        }

        @media (max-width: 600px) {
            .action-buttons {
                flex-direction: column;
            }

            /* Remove float and make image full width for mobile */
            .article-image {
                width: 100%;
                float: none;
                margin: 20px 0;
            }
        }
    </style>
  </head>
  <body>
    <div class="container">
        <div class="article-header">
            <h1 class="title"><%= @post[:title] %></h1>
            <p class="author-time">
            <span class="author"><%= URI(@post[:link]).host %></span> |
            <span class="time"><%= @post[:published_at].strftime('%d %b %Y %H:%M %Z') %></span>
            </p>
        </div>
        <% if @post[:image]%>
          <img src="<%= @post[:image] %>" alt="Article Image" class="article-image">
        <% end %>
        <p class="description">
          <%= @post[:description] %>
        </p>
        <div class="action-buttons">
          <form action="/posts/<%= @post[:id] %>/star" method="post">
            <input type="hidden" name="_method" value="PATCH">
            <% if @post[:starred_at] %>
              <button class="action-button star">⭐ Remove Star</button>
            <% else%>
              <button class="action-button star">⭐ Star</button>
            <% end %>
          </form>
          <form action="/posts/<%= @post[:id] %>/read_later" method="post">
            <input type="hidden" name="_method" value="PATCH">
            <% if @post[:read_later_at] %>
              <button class="action-button read-later">📑 Remove Read Later</button>
            <% else%>
              <button class="action-button read-later">📑 Read Later</button>
            <% end %>
          </form>
          <button class="action-button copy" onclick="navigator.clipboard.writeText('<%= @post[:link]%>')">📋 Copy to Clipboard</button>
          <a href="/posts/<%= @post[:id] %>/edit" class="action-button edit">✏️ Edit</a>
          <a href="<%= @post[:link]%>" target="_blank" class="action-button open-link">🔗 Open Article</a>
        </div>
    </div>
  </body>
</html>

